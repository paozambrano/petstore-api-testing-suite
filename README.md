# petstore-api-testing-suite
This project is a robust, data-driven automated testing framework for the Swagger Petstore REST API. It validates core CRUD operations, ensures API contract integrity via JSON Schema, and follows modern QA best practices like the Service Object Model (SOM).

